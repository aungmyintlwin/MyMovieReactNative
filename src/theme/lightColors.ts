import { DefaultTheme } from '@react-navigation/native';

const lightColorsTheme = {
    ...DefaultTheme.colors,
    textColor: '#000',
    background: 'rgb(100, 201, 125)',
    primary: 'rgb(44, 45, 85)'
}
export default lightColorsTheme;